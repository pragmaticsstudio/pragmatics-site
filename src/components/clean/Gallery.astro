---
interface Image {
	src: string;
	alt?: string;
}

interface Props {
	images: Image[];
}

const { images } = Astro.props;
---

<div class="clean-gallery">
	{images.map((image) => (
		<img src={image.src} alt={image.alt ?? ""} loading="lazy" />
	))}
</div>
